/**
 * Google Analytics 4 bootstrap (loaded from same origin so CSP need not allow inline script).
 * Loaded after gtag.js; keep in sync with index.html GA measurement ID.
 */
window.dataLayer = window.dataLayer || [];
function gtag() {
  dataLayer.push(arguments);
}
gtag('js', new Date());
gtag('config', 'G-PXCTXNN5KD');
